#include<stdio.h>
main()
{
    int n,r,sum=0;
    printf("enter the value of n\n");
    scanf("%d",&n);
    while(n>0)
    {
        r=n%10;
        sum=sum*10+r;
        n=n/10;
    }
    printf("The reverse of number is %d",sum);
}
